# findings.md

## 2026-03-03 문장 순환/분할 개선

### 결정
1. 분할은 문장 경계(`. ! ?`) 우선, 불가능하면 단어 단위로 폴백한다.
2. 길이 제한은 한글 90자, 영어 140자 유지.
3. 제출 시 큐 우선 소비, 큐 소진 후 API 재호출로 전환한다.

### 기대 효과
- 장문이 억지로 잘리는 체감 감소
- 같은 문단에서 자연스러운 순환 학습 가능

### 검증 결과
- `npm run lint`: 통과 (에러 0, 기존 경고 1건 유지: `src/components/WordChainGame.tsx:249`)
- `npm run test:run`: 통과 (12 files, 42 tests)
- `npm run build`: 통과
