# plan.md

## 구현 계획
1. `useUserStats`가 사용하는 점수 데이터 집계 로직을 순수 함수로 분리한다.
2. 동일 데이터에서 일일 미션 진행도와 스트릭 정보를 함께 계산한다.
3. 프로필 페이지에 `Daily Missions / Streak` 카드를 추가한다.
4. 계산 로직 회귀 테스트를 추가하고 문서 동기화, lint/test/build 검증을 수행한다.
