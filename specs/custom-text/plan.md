# 커스텀 텍스트 모드 — Plan

## 구현 계획
1. `custom_texts` 테이블/타입을 기준으로 사용자별 텍스트 CRUD 훅을 제공한다.
2. 문장연습 화면에서 `속담` / `내 텍스트` 소스 전환과 `localStorage` 복원을 지원한다.
3. 커스텀 텍스트는 현재 언어 기준으로만 노출하고, 비어 있으면 속담으로 폴백한다.
4. 관리 패널에서 추가/수정/삭제와 실패 메시지를 제공해 입력 흐름을 닫는다.
5. 커스텀 텍스트 선택/폴백/관리 에러 흐름을 테스트로 고정하고 문서를 동기화한다.
