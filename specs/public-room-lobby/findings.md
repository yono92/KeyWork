# findings.md

## 2026-03-12 조사 메모

### 확인된 상태
1. 현재 로비는 `빠른 매칭`, `방 만들기`, 대기방 목록, `코드로 참가`를 동시에 노출한다.
2. 실시간 목록 조회는 이미 동작하므로, 이번 변경은 주로 UX 단순화에 가깝다.

### 구현 메모
- 기존 Realtime hook과 join 로직은 유지하고 로비 노출 방식만 공개 목록 중심으로 줄인다.

## 2026-03-12 구현 메모

### 반영 내용
1. `MultiplayerLobby`에서 빠른 매칭, 코드 참가 입력, 방 코드 강조 UI를 제거했다.
2. 메인 액션을 `공개 방 만들기` 1개로 단순화하고, 설명 문구도 공개 목록형 입장 방식에 맞게 정리했다.
3. 대기 중 패널은 방 코드 대신 “공개 대기방” 상태와 목록 참가 안내를 보여주도록 바꿨다.

### 검증 결과
- `npm run lint`: 통과
- `npm run test:run`: 통과 (16 files, 68 tests)
- `npm run build`: 통과
