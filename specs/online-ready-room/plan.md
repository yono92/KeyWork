# plan.md

## 구현 계획
1. `useMultiplayerRoom`에 `myReady`, `opponentReady`, `setReady()`, 자동 시작 로직을 추가한다.
2. 온라인 페이지는 방 id가 생기면 즉시 게임룸 화면을 렌더링하도록 전환한다.
3. 테트리스 전투 화면은 대기 중에도 혼자 플레이할 수 있게 두고, 본게임 시작 시 보드/상대 상태를 리셋한다.
4. 끝말잇기 전투 화면은 프리게임 워밍업 입력 상태를 제공하고, 시작 시 공식 상태를 리셋한다.
5. 공통 `Ready` 패널을 만들어 두 게임에 같은 룸 UX를 적용한다.
6. 문서 동기화와 lint/test/build 검증을 수행한다.
