# plan.md

## 구현 계획
1. `userStats` 집계에 플레이 XP와 일일 미션 보너스 XP를 추가한다.
2. 총 XP를 기반으로 레벨/다음 레벨 요구 XP를 계산하는 헬퍼를 만든다.
3. `LevelBadge`를 실제 UI 컴포넌트로 구현하고 프로필 카드에 연결한다.
4. `오늘의 루프` 카드에 미션별 XP 보상과 오늘 획득 XP 요약을 추가한다.
5. 집계 테스트를 보강하고 문서를 동기화한다.
