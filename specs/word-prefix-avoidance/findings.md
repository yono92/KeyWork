# findings.md

## 2026-03-09 초기 메모

### 관찰
1. 현재 정책은 공유 접두 길이 중심이라 "이전 단어 + 확장 꼬리" 형태를 완전히 배제하지 못한다.
2. 특히 한국어 데이터 풀에서 이 체감이 크다.

### 메모
- `사과 -> 사과즙`, `학교 -> 학교생활` 같은 접두 확장 패턴을 직접 규칙에 포함한다.
- 데이터 풀이 작을 때는 기존처럼 폴백을 유지한다.

## 2026-03-09 구현 완료

### 반영 내용
1. `src/utils/wordDiversity.ts`
- 한국어 기준으로 이전 단어를 그대로 접두로 확장하는 패턴을 직접 차단
- 길이 3 이상 포함 관계도 추가 회피
- 점수화에서도 해당 패턴에 큰 패널티를 부여

2. 최근 단어 이력 확대
- `src/hooks/useKoreanWords.ts`
- `src/hooks/useWordChainGame.ts`
- 최근 단어 기억 창을 8개로 늘려 더 긴 반복 패턴을 회피

3. 테스트 보강
- `tests/utils/wordDiversity.test.ts`에 접두 확장/포함 패턴 회귀 테스트 추가

### 기대 변화
1. `사과 -> 사과즙`, `학교 -> 학교생활`처럼 바로 이어 붙는 출제가 훨씬 덜 나온다.
2. 작은 풀에서는 완전 차단 대신 우회 우선 선택 후 폴백하는 기존 동작은 유지된다.

### 검증 결과
- `npm run lint`: 통과
- `npm run test:run`: 통과 (14 files, 48 tests)
- `npm run build`: 통과
